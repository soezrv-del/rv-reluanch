node_modules/
.project_id
.github_repo
.env
.env.*
!.env.example

# Vercel Build Output API artifacts — committing these makes production
# skip `vite build` and ship stale UI. Keep .vercel/project.json if added.
.vercel/output/
.output/
dist/
