# Optional local-website template. Safe to commit — no secrets.
#
# The Android / iPhone app does NOT read this file.
# Point the phone at the live hosted site instead:
#
#   export CAP_SERVER_URL="https://YOUR-LIVE-APP.vercel.app"
#   npm run cap:sync:android
#
# Copy to `.env` only if you run the website on this computer.
# Put real API keys on the hosted app (Vercel), never in the Android APK.

# Live site the Capacitor WebView should open (no trailing slash).
CAP_SERVER_URL=

# Server-only. Live credit-union RV rates for GET /api/lenders.
# Free RateAPI tier is 20 requests / 30 days — do not commit a real key.
# RATEAPI_API_KEY=
#
# RATEAPI_MODE=live | simulate | off
# Unset defaults to simulate (no RateAPI calls — protects the free quota).
#   live     — call RateAPI when RATEAPI_API_KEY is set; else curated
#   simulate — demo lookup UX from the curated catalog; never calls RateAPI
#   off      — curated only, no pretend-live copy
# Set live on Production only when ready to spend quota.
# RATEAPI_MODE=simulate

# Server-only. YouTube Data API v3 for Facts opt-in RV Video Library match.
# Set on Vercel Production + Preview as YOUTUBE_API_KEY — never VITE_.
# Facts never calls YouTube until the user taps Yes.
# YOUTUBE_API_KEY=

# Mapbox — Trips visual basemap (streets/satellite) + optional geocode.
# David adds this on Vercel Production + Preview by hand — same pattern
# as YOUTUBE_API_KEY. No in-app Mapbox login.
# Use a default public token (pk.*) with URL restrictions. Truck routing
# stays HERE; this is not a Directions / Google router swap.
# MAPBOX_ACCESS_TOKEN=
# Optional public alias if you want the pk. token in the client bundle:
# VITE_MAPBOX_TOKEN=

# MarketCheck RV inventory search (server-only; never ship in the APK).
# Required on the hosted app (Vercel env vars) for Facts → Local inventory.
# Alias: MC_API_KEY. Personal plans typically cap search radius at 100 miles.
MARKETCHECK_API_KEY=
